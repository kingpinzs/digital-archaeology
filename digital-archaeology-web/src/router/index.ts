// src/router/index.ts
// Re-exports for router module (Story 11.7)

export { HashRouter, parseHash, buildHash } from './hashRouter';
export type { RouteState, RouteChangeCallback } from './hashRouter';
